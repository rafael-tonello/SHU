#!/bin/bash

#pwd is the project root
#arguments is the same passed to the command
#if the hooks returns (hooks is sourced) a non-zero exit code, the command will not be executed
#use _error to set error messages. You can contextualize the error using ':' (this happened: because this: do not do this)
